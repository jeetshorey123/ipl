# Supabase Project Credentials
SUPABASE_URL=https://xnfzscwsvxzotqrdpnqf.supabase.co
# Prefer service role on the server for Storage reads (keep secret)
SUPABASE_SERVICE_ROLE_KEY=
# Optional anon key if needed client-side
SUPABASE_ANON_KEY=
SUPABASE_BUCKET=ipl
# Empty prefix to scan entire bucket
SUPABASE_BUCKET_PREFIX=

# Flask
FLASK_ENV=production
PORT=5000
# Optional: Service role (NOT required for client-side)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PROJECT_ID=

# If your data is stored in Supabase Storage bucket as JSON files
# Set these to enable bucket-based loading (preferred)
SUPABASE_BUCKET=
# Optional subfolder within the bucket (e.g., 'matches' or 'json'); leave blank for root
SUPABASE_BUCKET_PREFIX=
